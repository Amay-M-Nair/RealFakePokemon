# Training / evaluation. The serving image installs requirements/serve.txt
# instead and deliberately never pulls torch -- see Dockerfile.
torch
torchvision
numpy
scipy          # DCT for pHash, matrix sqrt for FID
pillow
requests
tqdm
tensorboard
matplotlib     # notebooks only

# Export to ONNX (src/export_onnx.py)
onnx
onnxruntime
onnxconverter-common   # fp16 conversion
